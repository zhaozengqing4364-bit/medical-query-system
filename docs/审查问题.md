## 2026-03-01 二次全面审查回归修复

- [x] 修复 `embedding_batch.py` 队列处理 `claim_limit` 未定义导致的运行时崩溃
- [x] 修复 `embedding_batch.py` 的 `get_product_text_hash` 误用未定义 `batch_size`
- [x] 修复 `embedding_batch.py` 导入结果“全失败仍 success=true”的状态误报
- [x] 修复 `auto_sync.py` / `udid_server.py` 对批量导入失败未正确中断的问题
- [x] 修复 `sync_server.py`：`/api/status|logs|history|progress` 未鉴权可读
- [x] 修复 `sync_server.py`：CORS 从 `*` 调整为白名单 Origin
- [x] 修复 `sync_monitor.html`：移除 URL / localStorage 读取 `SYNC_API_KEY`，改为仅内存态输入
- [x] 修复 `admin.html` 同步监控请求未携带 `SYNC_API_KEY` 导致的鉴权断裂
- [x] 修复 `udid_server.py`：用户创建/重置密码缺少强口令校验
- [x] 修复 `udid_server.py`：登录接口缺少失败限流/锁定策略
- [x] 修复 `udid_server.py` / `sync_server.py` 多处内部异常明文回传
- [x] 修复 `udid_viewer.html` 属性上下文 XSS：移除内联事件拼接，改为 `data-* + addEventListener`

• A. Executive Summary

  - 审查方式：已按要求执行“主代理 -> 模块代理 -> 专项代理”并行审查。
    第一轮 spawn_agents_on_csv（6 模块）+ 第二轮 spawn_agents_on_csv（8 专项：安全/性能/测试）+ 1 个跨模块架构专项代理。
  - 总问题数（去重前）：96
    级别分布：Critical 14 / High 46 / Medium 34 / Low 2
  - 本轮输出策略：按你要求先给 Top 30 关键问题，后附模块补充清单（剩余 66 项）
  - Top 5 系统性风险：

  1. 默认凭证与 fail-open 鉴权（change-me / admin123 / 空 SYNC_API_KEY）导致未授权访问面暴露
  2. 前端多处存储型/属性型 XSS，且无 CSP/SRI 缓冲层
  3. 队列与事务语义不一致（非原子认领、时间窗回写、分批提交后“伪回滚”）
  4. 向量索引持久化可靠性不足（pickle 缺失、非原子写入、状态文件失配）
  5. 运维脚本存在高破坏动作默认值（drop/truncate、端口粗暴 kill -9、reload 无可验证契约）

  - 主代理冲突裁决与置信度：

  1. CORS + credentials 风险：静态证据明确，但未抓运行时响应头；最终裁决 High，置信度 0.78
  2. flock 解锁后删锁文件 竞态：静态语义成立，未做并发压测；最终裁决 Critical，置信度 0.82
  3. FAISS 构建一致性快照：静态显示缺事务快照，需运行态压测确认影响幅度；最终裁决 High，置信度 0.74

  ———

  B. Findings（Top 30）

  1. 弱默认 Session 密钥可伪造会话 | Critical | M2
     证据：udid_server.py:106, udid_server.py:107；命令摘要：rg -n "change-me|ADMIN_DEFAULT_PASSWORD|SYNC_API_KEY"
     udid_server.py sync_server.py 命中弱默认值。
     影响：攻击者可伪造管理员 session。
     复现方式：不设置 SECRET_KEY 启动后构造 Flask session cookie。
     修复建议：生产强制 fail-fast，无密钥或弱值直接拒绝启动；密钥轮换机制。
     预估工作量：S
  2. 默认管理员密码 admin123 | Critical | M2
     证据：udid_server.py:147；命令摘要：同上。
     影响：首次部署可被弱口令秒破。
     复现方式：空用户库启动后用 admin/admin123 登录。
     修复建议：首次启动强制注入强密码，禁止默认口令。
     预估工作量：S
  3. 同步服务鉴权 fail-open（空 SYNC_API_KEY 直接放行） | Critical | M2
     证据：sync_server.py:48, sync_server.py:214；命令摘要：nl -ba sync_server.py | sed -n '206,236p'。
     影响：未授权可触发敏感同步写操作。
     复现方式：不配置 SYNC_API_KEY，POST /api/sync/start。
     修复建议：生产环境密钥必填，缺失即启动失败。
     预估工作量：S
  4. --pipeline-reset 引用未定义常量导致崩溃 | Critical | M1
     证据：embedding_batch.py:1637, embedding_batch.py:33；命令摘要：rg -n "PIPELINE_STATE_FILE|PIPELINE_STATE_DB"
     embedding_batch.py。
     影响：故障恢复入口不可用。
     复现方式：运行 python3 embedding_batch.py --pipeline-reset。
     修复建议：改为使用已定义常量（PIPELINE_STATE_DB/LEGACY_PIPELINE_STATE_FILE）。
     预估工作量：S
  5. pickle 未导入却直接调用 | Critical | M3
     证据：embedding_faiss.py:91, embedding_faiss.py:111, embedding_faiss.py:15；命令摘要：rg -n "pickle|import pickle"
     embedding_faiss.py。
     影响：索引加载/保存路径确定性失败。
     复现方式：调用索引加载/保存流程。
     修复建议：显式 import pickle，并加启动自检。
     预估工作量：S
  6. 用户管理表存储型 XSS（未转义写入 innerHTML） | Critical | M4
     证据：admin.html:1255, admin.html:1267；命令摘要：nl -ba admin.html | sed -n '1248,1272p'。
     影响：管理员页可被持久化脚本劫持。
     复现方式：构造恶意用户名后进入用户列表页。
     修复建议：全部改 textContent/DOM API 渲染，禁用内联拼接。
     预估工作量：M
  7. udid_viewer 属性上下文 XSS（单引号未转义） | Critical | M4
     证据：udid_viewer.html:1293, udid_viewer.html:1980；命令摘要：nl -ba udid_viewer.html | sed -n '1270,1312p' 与 sed -n
     '1978,1994p'。
     影响：可注入事件属性执行任意 JS。
     复现方式：制造含 ' 的厂商名，触发按钮 hover/click。
     修复建议：统一转义函数，补 &#39;，取消内联事件。
     预估工作量：M
  8. 运维存在弱默认数据库凭据回退 | Critical | M5
     证据：scripts/migrate.sh:14, scripts/db_postgres.py:34；命令摘要：rg -n "your_secure_password|POSTGRES_PASSWORD" scripts/
     migrate.sh scripts/db_postgres.py。
     影响：误部署即暴露数据库。
     复现方式：未设置环境变量执行迁移脚本。
     修复建议：移除回退默认值，缺失即退出。
     预估工作量：S
  9. 模板敏感键是“可生效占位值” | Critical | M6
     证据：.env.template:41, .env.template:49, gaoxin-medical.service:12；命令摘要：nl -ba .env.template 与 nl -ba gaoxin-
     medical.service。
     影响：占位符被当真值上线。
     复现方式：直接复制模板并启动。
     修复建议：模板置空 + 启动时强校验非占位值。
     预估工作量：S
  10. _sync_progress 填入 datetime 可能触发 JSON 序列化失败 | Critical | M2
     证据：sync_server.py:77；命令摘要：专项代理复核 + 静态定位。
     影响：关键同步接口在特定路径返回 500。
     复现方式：触发进度更新后访问进度接口。
     修复建议：统一序列化为 ISO8601 字符串。
     预估工作量：S
  11. 锁释放后删除锁文件存在竞态窗口 | Critical | M1
     证据：auto_sync.py:128, auto_sync.py:137；命令摘要：nl -ba auto_sync.py | sed -n '112,145p'。
     影响：可能出现多实例并发同步。
     复现方式：并发启动多个同步进程抢锁。
     修复建议：不删除锁文件，仅 LOCK_UN；或改为稳定 inode 锁协议。
     预估工作量：M
  12. /api/parse-bid 无登录校验可匿名触发外部 AI | High | M2
     证据：udid_server.py:1293, udid_server.py:1314；命令摘要：nl -ba udid_server.py | sed -n '1288,1335p'。
     影响：费用滥用、数据外送风险。
     复现方式：匿名 POST /api/parse-bid。
     修复建议：至少 _require_login() + 限流配额。
     预估工作量：S
  13. 多个接口未统一鉴权边界 | High | M2
     证据：udid_server.py:463（示例）；命令摘要：rg -n "_require_login|_require_admin|@app.route" 对比路由覆盖。
     影响：产生“同域不同安全级”漏洞。
     复现方式：逐接口抓取是否返回未授权。
     修复建议：路由分组级鉴权中间件。
     预估工作量：M
  14. /api/sync/full|data|vectors 实际同一执行路径 | High | M2
     证据：sync_server.py:278, sync_server.py:374, sync_server.py:376；命令摘要：nl -ba sync_server.py | sed -n '260,460p'。
     影响：接口契约名实不符，调用方易误用。
     复现方式：分别调用三个端点对比行为。
     修复建议：实现真正分流或收敛为单端点。
     预估工作量：M
  15. embedding_service.get_embeddings 重试机制被吞错短路 | High | M2
     证据：embedding_service.py:186, embedding_service.py:252, embedding_service.py:277；命令摘要：nl -ba embedding_service.py |
     sed -n '160,292p'。
     影响：瞬时网络波动直接失败。
     复现方式：注入临时网络错误观察无 backoff 重试。
     修复建议：可重试异常向上抛给装饰器。
     预估工作量：S
  16. CORS + Cookie 会话且无 CSRF 防护策略 | High | M2
     证据：udid_server.py:35, admin.html:547；命令摘要：rg -n "CORS\\(|credentials: 'include'" udid_server.py admin.html。
     影响：跨站请求攻击面扩大。
     复现方式：已登录浏览器发起跨站 fetch。
     修复建议：Origin 白名单 + CSRF token。
     预估工作量：M
  17. 队列完成判定用“最近1小时 embeddings”存在误标 | High | M1
     证据：auto_sync.py:403, auto_sync.py:408；命令摘要：nl -ba auto_sync.py | sed -n '392,414p'。
     影响：非本批次任务被误置 completed。
     复现方式：并行写入 embeddings 后执行 batch。
     修复建议：按本批 di_code 集合精确回写。
     预估工作量：M
  18. 队列消费无原子认领，存在并发重复处理 | High | M1
     证据：embedding_batch.py:1687, embedding_batch.py:1695；命令摘要：nl -ba embedding_batch.py | sed -n '1680,1838p'。
     影响：重复调用外部 embedding API、成本翻倍。
     复现方式：双进程同时消费 pending 队列。
     修复建议：pending->processing 原子 update-returning 认领。
     预估工作量：M
  19. 断点续传在服务端忽略 Range 时会损坏 ZIP | High | M1
     证据：udid_sync.py:253, udid_sync.py:279；命令摘要：专项 + 代码分支审查。
     影响：下载内容拼接重复，后续解压/导入失败。
     复现方式：服务端对 Range 返回 200 全量响应。
     修复建议：resume_bytes>0 && status=200 时改 wb 重下。
     预估工作量：S-M
  20. 分批提交后异常仅回滚最后事务，状态失真 | High | M1
     证据：udid_hybrid_system.py:399, udid_hybrid_system.py:444, udid_hybrid_system.py:449；命令摘要：nl -ba
     udid_hybrid_system.py | sed -n '360,462p'。
     影响：日志显示失败但数据已部分入库。
     复现方式：在中间批次后注入异常。
     修复建议：全量单事务或显式 partial-success 审计模型。
     预估工作量：M
  21. cancel_batch 缺 requests 导入，取消路径失效 | High | M1
     证据：auto_sync.py:240, auto_sync.py:25；命令摘要：rg -n "requests.post|import requests" auto_sync.py。
     影响：超时批任务无法取消，外部计费持续。
     复现方式：调用 cancel_batch。
     修复建议：补导入并细化异常处理。
     预估工作量：S
  22. 索引与映射非原子落盘 + 无一致性校验 | High | M3
     证据：embedding_faiss.py:109, embedding_faiss.py:111, embedding_faiss.py:89；命令摘要：nl -ba embedding_faiss.py | sed -n
     '82,116p'。
     影响：中断时可出现撕裂快照。
     复现方式：写入中途 kill 进程后重启加载。
     修复建议：临时文件 + manifest + rename 原子切换。
     预估工作量：M
  23. 批处理状态标记 imported，但文件系统对应结果大量缺失 | High | M3
     证据：data/embedding_batch/pipeline_state.json, data/embedding_batch；命令摘要：本地校验脚本统计 51 imported 与 51
     result_path missing。
     影响：状态机不可置信，恢复流程失真。
     复现方式：读取 state 后逐条检查 result_path 是否存在。
     修复建议：启动一致性校验 + 修复工具（reconcile）。
     预估工作量：M-L
  24. 索引构建 fetchall + list + np.array 内存峰值过高 | High | M3
     证据：embedding_faiss.py:169, embedding_faiss.py:179；命令摘要：专项性能代理复核。
     影响：大规模构建时 OOM 风险。
     复现方式：大数据量触发训练构建。
     修复建议：流式批迭代训练/构建。
     预估工作量：M
  25. 审计表渲染未转义（存储型 XSS） | High | M4
     证据：admin.html:1343, admin.html:1351；命令摘要：nl -ba admin.html | sed -n '1338,1356p'。
     影响：日志注入可劫持后台。
     复现方式：构造恶意 action/ip 入审计表。
     修复建议：DOM 安全渲染 + 输出编码。
     预估工作量：S
  26. sync_monitor 历史表拼接 innerHTML 未转义 | High | M4
     证据：sync_monitor.html:486, sync_monitor.html:499；命令摘要：nl -ba sync_monitor.html | sed -n '482,504p'。
     影响：监控页面可被注入脚本。
     复现方式：让 message 带 HTML/JS。
     修复建议：模板变量转义或 textContent。
     预估工作量：S
  27. 前端缺 CSP 且依赖 CDN 脚本无 SRI | High | M4
     证据：admin.html:10, login.html:13, udid_viewer.html:18；命令摘要：rg -n "cdn.tailwindcss.com|Content-Security-Policy|
     integrity=" ...。
     影响：XSS/供应链风险无浏览器层缓冲。
     复现方式：第三方脚本篡改或注入链路触发。
     修复建议：本地化静态资源 + CSP nonce/hash + SRI。
     预估工作量：M
  28. 迁移配置默认包含 drop/truncate 破坏动作 | High | M5
     证据：scripts/migration.load:12, scripts/migration.load:50；命令摘要：nl -ba scripts/migration.load | sed -n '1,60p'。
     影响：误执行可清空生产表。
     复现方式：直接运行迁移配置。
     修复建议：默认非破坏模式，破坏动作需显式开关+二次确认。
     预估工作量：M
  29. 启动脚本按端口直接 kill -9 | High | M5
     证据：start_sync_server.sh:11, start_sync_server.sh:14；命令摘要：nl -ba start_sync_server.sh | sed -n '1,22p'。
     影响：误杀非目标服务，破坏同机稳定性。
     复现方式：8080 被其他进程占用时执行脚本。
     修复建议：校验进程归属+优雅停止+超时后再 kill。
     预估工作量：S
  30. logrotate reload 与 service 契约不一致；迁移校验不设失败门禁 | High | M5
     证据：logrotate-gaoxin-medical:11, gaoxin-medical.service, scripts/migrate.sh:135；命令摘要：rg -n "ExecReload" gaoxin-
     medical.service logrotate-gaoxin-medical 无命中 + nl -ba scripts/migrate.sh | sed -n '118,165p'。
     影响：轮转动作不可验证；数据校验不一致也可能继续流程。
     复现方式：执行 logrotate/迁移流程观察无硬失败。
     修复建议：补 ExecReload 或改 postrotate 策略；校验失败 exit 1。
     预估工作量：S-M

  修复勾选（2026-03-01）：
  - [x] 1 弱默认 Session 密钥可伪造会话
  - [x] 2 默认管理员密码 admin123
  - [x] 3 同步服务鉴权 fail-open（空 SYNC_API_KEY）
  - [x] 4 --pipeline-reset 未定义常量崩溃
  - [x] 5 pickle 未导入却直接调用
  - [x] 6 用户管理表存储型 XSS
  - [x] 7 udid_viewer 属性上下文 XSS（单引号）
  - [x] 8 运维弱默认数据库凭据回退
  - [x] 9 模板敏感键可生效占位值
  - [x] 10 _sync_progress datetime 序列化风险
  - [x] 11 锁释放后删除锁文件竞态
  - [x] 12 /api/parse-bid 无登录校验
  - [x] 13 多接口鉴权边界不统一
  - [x] 14 /api/sync/full|data|vectors 契约塌缩
  - [x] 15 embedding_service 重试机制被吞错短路
  - [x] 16 CORS + Cookie 缺 CSRF 防护
  - [x] 17 队列完成判定“最近1小时 embeddings”误标
  - [x] 18 队列消费无原子认领
  - [x] 19 断点续传 Range=200 响应损坏 ZIP
  - [x] 20 分批提交后异常仅回滚最后事务
  - [x] 21 cancel_batch 缺 requests 导入
  - [x] 22 索引与映射非原子落盘
  - [x] 23 批处理状态 imported 与 result_path 失配
  - [x] 24 索引构建 fetchall 内存峰值高
  - [x] 25 审计表渲染未转义 XSS
  - [x] 26 sync_monitor 历史表未转义 XSS
  - [x] 27 缺 CSP 且外部脚本供应链风险
  - [x] 28 迁移配置默认含破坏动作
  - [x] 29 启动脚本端口占用直接 kill -9
  - [x] 30 logrotate reload 契约不一致 / 迁移校验无失败门禁

  补充清单（剩余 66 项，按模块）：

  - M1：13 项（资源释放、吞错、可测试性、轮询耦合）
  - M2：12 项（错误码契约、布尔参数语义、信息泄露、幂等不足）
  - M3：9 项（检索参数上限、训练样本偏置、内存/CPU开销）
  - M4：11 项（输入校验不足、轮询并发、维护性问题）
  - M5：16 项（最小权限、备份可恢复性、发布回滚缺口）
  - M6：5 项（依赖治理、配置契约漂移、来源不透明）

  ———

  C. Module Scorecard

  1. M1 评分 4.7/10
     扣分项：

  - Critical：CLI 恢复分支崩溃（embedding_batch.py:1637）
  - High：队列一致性缺陷（auto_sync.py:403, embedding_batch.py:1687）
  - High：事务语义失真（udid_hybrid_system.py:399, udid_hybrid_system.py:444）
    最先修 3 项：
  - 修复 PIPELINE_STATE_FILE 未定义
  - 队列改原子认领 + 精确回写
  - 修复断点续传与锁文件竞态

  2. M2 评分 3.8/10
     扣分项：

  - Critical：默认凭证/密钥（udid_server.py:106, udid_server.py:147, sync_server.py:48）
  - High：鉴权边界不统一（udid_server.py:1293）
  - High：接口契约塌缩（sync_server.py:278, sync_server.py:374）
    最先修 3 项：
  - 强制安全启动（密钥/密码必填）
  - /api/parse-bid 与高成本接口鉴权+限流
  - 修复同步接口语义与错误码契约

  3. M3 评分 4.6/10
     扣分项：

  - Critical：pickle 导入缺失（embedding_faiss.py:91）
  - High：索引持久化非原子（embedding_faiss.py:109）
  - High：状态文件与实盘失配（data/embedding_batch/pipeline_state.json）
    最先修 3 项：
  - 修复 pickle 并加自检
  - 引入原子索引快照机制
  - 建立 state reconcile 工具

  4. M4 评分 4.6/10
     扣分项：

  - Critical：用户表和结果表 XSS（admin.html:1255, udid_viewer.html:1293）
  - High：审计与监控历史 XSS（admin.html:1343, sync_monitor.html:486）
  - High：无 CSP/SRI（admin.html:10）
    最先修 3 项：
  - 移除 innerHTML + inline onclick
  - 统一 escape 策略并覆盖单引号
  - 上 CSP/SRI 并本地化关键依赖

  5. M5 评分 3.9/10
     扣分项：

  - Critical：弱默认凭据回退（scripts/migrate.sh:14）
  - High：破坏性迁移默认开启（scripts/migration.load:12）
  - High：kill -9 误杀风险（start_sync_server.sh:14）
    最先修 3 项：
  - 秘钥与凭据 fail-fast
  - 迁移流程加保护闸和硬失败门禁
  - 启停/监控改可验证、可回滚

  6. M6 评分 4.8/10
     扣分项：

  - Critical：模板占位敏感值可直接生效（.env.template:41）
  - High：配置默认值漂移（config.json:3, config.json.template:6）
  - High：依赖未锁定可复现（requirements.txt:8）
    最先修 3 项：
  - 配置 schema + 启动校验
  - 依赖锁定与供应链校验
  - 统一配置来源优先级

  ———

  D. Optimization Roadmap（重点）

  1. M1 核心同步与批处理
     当前瓶颈：队列状态机与事务语义不一致，导致并发与恢复风险。
     7天内可落地：修复 --pipeline-reset、cancel_batch、断点续传策略、锁文件释放策略；前置条件：明确当前生产执行入口；潜在回归风
     险：老脚本参数兼容性；验收指标：关键命令成功率 100%，批任务重复处理率下降到 0。
     30天结构优化：队列改“原子认领 + 精确回写 + 幂等键”；前置条件：表结构允许 processing 状态；潜在回归风险：旧任务状态迁移；验
     收指标：并发双实例下无重复外部 API 调用。
     90天演进：统一调度平面（sync/vector/batch 一体任务状态中心）；前置条件：跨模块接口契约冻结；潜在回归风险：部署切换窗口抖
     动；验收指标：状态一致性告警归零、故障恢复时长下降 50%。
  2. M2 服务网关与 API
     当前瓶颈：安全基线弱、鉴权边界不一致、契约语义不稳。
     7天内可落地：密钥/默认口令 fail-fast、/api/parse-bid 加鉴权、SYNC_API_KEY 必填；前置条件：运维密钥注入方案确定；潜在回归风
     险：旧客户端认证失败；验收指标：未授权请求全部返回 401/403。
     30天结构优化：拆分鉴权中间件与统一错误码层，修复 /full|data|vectors 契约；前置条件：API 版本策略；潜在回归风险：前端适配成
     本；验收指标：契约测试通过率 100%。
     90天演进：udid_server 按子域拆 Blueprint（auth/search/sync/embedding/config）；前置条件：模块边界定义；潜在回归风险：路由迁
     移；验收指标：单模块改动影响面下降（回归缺陷率下降 40%）。
  3. M3 向量索引与状态一致性
     当前瓶颈：索引持久化原子性与批状态可信度不足。
     7天内可落地：补 import pickle、加 ntotal == len(id_map) 启动校验、state 文件巡检脚本；前置条件：可接受一次性巡检；潜在回归
     风险：历史脏数据触发报警；验收指标：索引加载失败率归零。
     30天结构优化：索引快照采用 index.tmp + map.tmp + manifest 原子切换；前置条件：磁盘空间预留；潜在回归风险：切换窗口读写冲
     突；验收指标：异常中断后可自动回滚到上个快照。
     90天演进：构建流程流式化与增量重建；前置条件：任务编排支持；潜在回归风险：召回精度波动；验收指标：构建内存峰值下降 40%，构
     建耗时下降 30%。
  4. M4 前端管理与监控
     当前瓶颈：XSS 高危点多，渲染与安全策略不统一。
     7天内可落地：替换所有高危 innerHTML/内联事件路径，修复 escapeHtml 单引号缺失；前置条件：后端字段不变；潜在回归风险：交互事
     件绑定遗漏；验收指标：恶意 payload 回显不执行。
     30天结构优化：上 CSP（nonce/hash）+ 本地化 Tailwind 产物 + SRI；前置条件：构建流程支持静态产物；潜在回归风险：旧内联脚本失
     效；验收指标：浏览器安全头覆盖率 100%。
     90天演进：前端渲染层组件化（统一编码/校验/节流）；前置条件：页面模块拆分；潜在回归风险：样式回归；验收指标：前端安全扫描高
     危 0，页面首屏稳定。
  5. M5 运维部署与可观测性
     当前瓶颈：脚本破坏动作与恢复链路不闭环。
     7天内可落地：禁止默认 DB 密码、迁移校验失败硬退出、替换端口粗暴 kill -9；前置条件：发布窗口；潜在回归风险：旧脚本中断；验收
     指标：迁移/启动失败可明确阻断。
     30天结构优化：备份从“可压缩”升级到“可恢复演练”，监控加入重启节流与锁；前置条件：演练环境；潜在回归风险：恢复流程首次磨合；
     验收指标：月度恢复演练成功率 100%。
     90天演进：发布流程事务化（预检->发布->验证->回滚）；前置条件：CI/CD 介入；潜在回归风险：流程学习成本；验收指标：发布失败自
     动回滚成功率 >95%。
  6. M6 配置与依赖治理
     当前瓶颈：配置契约漂移、依赖不可复现。
     7天内可落地：模板置空敏感项 + 启动校验占位值 + 统一环境优先级；前置条件：配置 owner 明确；潜在回归风险：旧环境启动失败（这
     是期望 fail-fast）；验收指标：占位值启动拦截率 100%。
     30天结构优化：引入配置 schema 校验与依赖锁文件；前置条件：包管理策略确定；潜在回归风险：升级冲突；验收指标：环境重建可复现
     率 100%。
     90天演进：配置中心化（版本化+审计）；前置条件：跨模块接入；潜在回归风险：迁移窗口；验收指标：配置变更可追溯率 100%。

  ———

  E. Priority Plan

  P0（立即）

  1. 安全基线止血：移除 change-me/admin123、强制 SYNC_API_KEY、修复 M4 高危 XSS。
     收益：直接封堵最高攻击面；复杂度：中；依赖：运维密钥注入、前后端联动；负责人画像：后端安全 + 前端资深 + 运维。
  2. 可用性止血：修复 PIPELINE_STATE_FILE 未定义、cancel_batch 导入缺失、pickle 缺失。
     收益：恢复关键恢复/索引能力；复杂度：低；依赖：无；负责人画像：后端。
  3. 运维破坏动作收敛：禁用默认破坏迁移、移除端口粗暴 kill -9。
     收益：降低生产事故概率；复杂度：低中；依赖：发布脚本变更；负责人画像：运维。

  P1（2-4 周）

  1. 队列一致性改造：原子认领、精确回写、幂等键。
     收益：降低重复处理和计费；复杂度：中；依赖：数据库迁移；负责人画像：后端。
  2. API 契约整治：同步端点语义对齐、错误码分层。
     收益：减少调用方误用和回归；复杂度：中；依赖：前端接口适配；负责人画像：后端 + 前端。
  3. 前端安全工程化：CSP/SRI/依赖本地化。
     收益：系统性降低 XSS 与供应链风险；复杂度：中；依赖：构建流程；负责人画像：前端平台工程师。

  P2（1-3 月）

  1. 架构拆分：udid_server 按 bounded context 拆分。
     收益：长期可维护性提升；复杂度：高；依赖：接口冻结；负责人画像：架构师 + 后端。
  2. 统一任务控制平面：状态中心与调度统一。
     收益：可观测性与恢复能力提升；复杂度：高；依赖：跨模块改造；负责人画像：后端架构 + SRE。
  3. 配置与依赖治理体系化。
     收益：环境可复现、变更可审计；复杂度：中高；依赖：工程规范；负责人画像：平台工程师。

  ———

  F. Verification Gaps

  1. 并发竞态类（锁文件竞态、队列重复处理、跨进程互斥）
     未验证原因：未执行运行时并发压测（本轮按要求以静态为主、最小验证）。
     补充建议：在隔离环境做 2-4 实例并发脚本压测，记录重复任务率/锁冲突率。
  2. CORS 运行时行为
     未验证原因：未抓真实响应头与浏览器行为。
     补充建议：抓包验证 Access-Control-* 头组合，并做跨站 PoC。
  3. FAISS 构建一致性/性能量化
     未验证原因：未运行大规模构建与故障注入。
     补充建议：用生产同量级样本做构建基准，记录内存峰值与恢复行为。
  4. 备份恢复闭环
     未验证原因：仅看到脚本“打包可读”，未执行恢复演练。
     补充建议：按月执行“备份->恢复->验数->抽样查询”演练并留审计记录。
  5. 前端 XSS PoC 浏览器实证
     未验证原因：本轮是静态证据与渲染路径审查。
     补充建议：用 Playwright 做 4 个页面 payload 注入验证并截图留档。

  ———
