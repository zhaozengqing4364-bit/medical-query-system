# 全量修复计划与执行记录（2026-03-01）

## 状态看板
- 总项：92
- 已完成：92
- 进行中：0
- 阻塞：0

## 修复清单（执行即打标）

### A. 同步链路与并发控制
- [x] BUG-001 `udid_sync.py`：`_report_progress` 不再吞掉 `RuntimeError`，停止信号可透传中断。
- [x] BUG-002 `udid_hybrid_system.py`：`sync_log` 增加 `data_date` 字段并兼容历史库结构。
- [x] BUG-003 `udid_hybrid_system.py`：导入记录补写 `data_date`（优先从 `YYYYMMDD` 文件名解析，失败回退当天）。
- [x] BUG-004 `udid_hybrid_system.py`：`get_last_sync_date()` 优先使用 `data_date`，避免执行日期误推进同步水位。
- [x] BUG-005 `udid_hybrid_system.py`：`embedding_update_queue` 增加 `claimed_at` 字段并自动迁移。
- [x] BUG-006 `sync_server.py`：全局状态/进度读写改为 `RLock`，覆盖 `update/reset/status/progress/stop` 并发访问。
- [x] BUG-007 `sync_server.py`：同步进度与历史时间统一为 UTC（`timezone.utc`）。
- [x] BUG-008 `sync_server.py`：启动同步前接入跨进程文件锁，执行结束后释放；线程启动失败时回滚状态并释放锁。

### B. 自动同步与批处理流程
- [x] BUG-009 `auto_sync.py`：`run_vector_sync_batch()` 增加 `should_stop` 回调，实现可中断等待。
- [x] BUG-010 `auto_sync.py`：Batch 终态加入 `error` 失败判定，避免“异常状态持续等待到超时”。
- [x] BUG-011 `auto_sync.py`：数据同步失败时立即中断后续向量阶段（Fail-Fast）。
- [x] BUG-012 `auto_sync.py`：CLI 模式（`--quick/--batch/--data-only/--vectors-only`）统一加锁，防止绕锁并发。
- [x] BUG-013 `embedding_batch.py`：变更检测去除“7天+50000”硬窗口，改为全量候选分页扫描。
- [x] BUG-014 `embedding_batch.py`：待导入任务查询去时间窗限制，覆盖全部未导入任务。
- [x] BUG-015 `embedding_batch.py`：`wait_for_completion()` / `pipeline_check_status()` 统一使用 `check_batch_status()` 并将 `error` 视为失败终态。
- [x] BUG-016 `embedding_batch.py`：下载结果支持 DashScope `output_url` 直链（`download_result_url`）。
- [x] BUG-017 `embedding_batch.py`：导入状态仅在“明确导入成功”时标记 `imported`，否则标记 `failed` 并保留结果文件用于重试。
- [x] BUG-018 `embedding_batch.py`：队列 stale 恢复改为 `COALESCE(claimed_at, created_at)` 判断，并在认领时回写 `claimed_at`。
- [x] BUG-019 `embedding_batch.py`：`pipeline_generate_all` 批次状态初始化补齐 `output_url: None`，避免状态字段不完整。
- [x] BUG-020 `embedding_batch.py`：CLI `--status` 查询改为 `check_batch_status()`，统一状态口径。

### C. 向量服务与索引可靠性
- [x] BUG-021 `embedding_service.py`：OpenAI 响应结构严格校验（`data` / `embedding`），异常明确抛出。
- [x] BUG-022 `embedding_service.py`：`get_single_embedding()` 扩展异常捕获（`ValueError/TypeError/KeyError/IndexError`）。
- [x] BUG-023 `embedding_service.py`：向量重排前增加维度一致性校验，维度不一致记录并跳过。
- [x] BUG-024 `embedding_faiss.py`：移除 `pickle.load` 读取 `id_map.pkl`，改为 `id_map.json`。
- [x] BUG-025 `embedding_faiss.py`：遇到遗留 `id_map.pkl` 明确拒绝加载并提示重建，消除反序列化风险。
- [x] BUG-026 `embedding_faiss.py`：元数据查询改分批 `IN`，规避 SQLite 变量上限导致的查询失败。

### D. 主服务 API 契约与安全边界
- [x] BUG-027 `udid_server.py`：新增 `/api/sync/*` 代理层（`start/stop/full/data/vectors/progress/history/status/logs`），同步服务不可用返回 502 JSON，避免前端 404。
- [x] BUG-028 `udid_server.py`：请求级 DB 串行锁（`before_request` / `teardown_request`）缓解全局 SQLite 并发冲突。
- [x] BUG-029 `udid_server.py`：`_ensure_default_admin()` 仅吞并发唯一约束冲突，其他异常保留 fail-fast。
- [x] BUG-030 `udid_server.py`：`/api/auth/logout` 增加登录校验与 CSRF 校验。
- [x] BUG-031 `udid_server.py`：`/api/stats` 去登录强制，允许登录页匿名拉取统计。
- [x] BUG-032 `udid_server.py`：`/api/upload` 缺失 `file` 字段时返回 400。
- [x] BUG-033 `udid_server.py`：`/api/algo-match` 坏 JSON 与非对象请求体统一返回 400。
- [x] BUG-034 `udid_server.py`：`/api/ai-match` FTS 降级 SQL 修复为 `products_fts MATCH ?`；`/api/search` 移除固定 5000 上限避免深分页截断。

### E. 前端交互与渲染安全
- [x] BUG-035 `admin.html`：日志响应判定改为 `Array.isArray(logsRes.logs)`，避免空对象误判。
- [x] BUG-036 `admin.html`：删除用户按钮由内联 `onclick` 改为 `data-* + addEventListener`，并补 `escapeHtmlAttr` 与 `escapeJsString` 双引号转义。
- [x] BUG-037 `sync_monitor.html`：`fetchStatus()` 异常不再伪造“已完成”，返回 `_fetch_error + is_syncing:null` 并保持轮询。
- [x] BUG-038 `sync_monitor.html`：覆盖率统计增加除零保护（`0.0%`）。
- [x] BUG-039 `udid_viewer.html`：同步结果按 `data.success` 分支展示，失败不再显示“同步完成”。
- [x] BUG-040 `udid_viewer.html`：`testAiConnection(event)` 显式接收事件对象，移除对全局 `event` 的依赖。
- [x] BUG-041 `login.html`：移除密码框 Enter 二次触发 click 逻辑，避免重复提交。

### F. 部署与运维脚本
- [x] BUG-042 `sync_server.py` / `start_sync_server.sh`：同步监控服务端口统一为 `8888`。
- [x] BUG-043 `nginx-gaoxin-medical.conf`：新增 `/api/sync/*`、`/api/status`、`/api/logs` 到 `127.0.0.1:8888` 分流，其他 `/api/*` 保持 `8080`。
- [x] BUG-044 `deploy.sh`：Nginx 模板同步分流规则。
- [x] BUG-045 `deploy.sh`：新增独立 systemd 服务 `${SERVICE_NAME}-sync`，并与主服务一起 enable/start/status。
- [x] BUG-046 `start_sync_server.sh`：移除默认 `kill -9` 升级策略，改为优雅停止失败即退出并提示人工处理，避免破坏性动作。
- [x] BUG-047 `deploy.sh`：logrotate `postrotate` 改为 `reload 失败不阻断`，避免服务不支持 reload 时影响日志轮转。

### G. 复用与可维护性收敛（2026-03-02）
- [x] BUG-048 `config_utils.py`：抽取 `.env/JSON/SQLite system_config/env` 统一配置合并能力，并在 `ai_service.py`、`embedding_service.py`、`embedding_batch.py` 接入，保持原优先级语义（`defaults < file < db < env`）。
- [x] BUG-049 `retry_utils.py`：抽取共享指数退避重试装饰器，替换 `embedding_service.py` 与 `embedding_batch.py` 重复实现，统一可重试状态码判定与抖动策略。

### H. PostgreSQL 全量切换（2026-03-02）
- [x] BUG-050 `udid_server.py`：搜索链路增加 PostgreSQL 兼容分支，PostgreSQL 运行时不再依赖 `sqlite_master/products_fts MATCH/rowid`。
- [x] BUG-051 `udid_server.py`：`/api/algo-match` 与 `/api/ai-match` FTS 降级路径支持 PostgreSQL `ILIKE` 关键词召回，保持响应契约不变。
- [x] BUG-052 `embedding_service.py`：`vector_search` 在 PostgreSQL 下改为 `ILIKE` 粗召回 + 向量重排，保留 SQLite FTS 回滚分支。
- [x] BUG-053 `auto_sync.py` / `sync_server.py` / `embedding_batch.py`：修复 PostgreSQL 不兼容 SQL（`status="..."`、`datetime('now', '-2 hours')`、`BEGIN IMMEDIATE`）。
- [x] BUG-054 `.env.template` / `config.json.template` / `requirements.txt`：补齐 `DB_BACKEND`、`POSTGRES_*` 配置与 `psycopg2-binary` 依赖。
- [x] BUG-055 `deploy.sh` / `monitor.sh` / `backup.sh`：部署、监控、备份脚本切换为 PostgreSQL 健康检查与 `pg_dump` 备份，保留 SQLite 回滚分支。
- [x] BUG-056 `scripts/setup_postgres.sql` / `scripts/migrate.sh`：PostgreSQL 初始化与迁移脚本对齐运行时 schema（补齐 `embeddings.embedding`、`sync_log.data_date`、`embedding_update_queue.claimed_at/processed_at` 等关键字段）。

### I. PostgreSQL 并发查询能力释放（2026-03-02）
- [x] BUG-057 `db_backend.py`：PostgreSQL 连接改为 `ThreadedConnectionPool`，新增 `POSTGRES_POOL_MIN/MAX` 参数，连接归还前自动 `rollback` 清理事务，避免并发下 `idle in transaction` 污染。
- [x] BUG-058 `udid_hybrid_system.py`：`LocalDataLake` 增加 PostgreSQL 线程隔离连接（thread-local），避免多线程共享同一连接导致并发冲突。
- [x] BUG-059 `udid_server.py` / `.env.template`：请求级全局 DB 锁改为仅 SQLite 启用；PostgreSQL 请求结束释放线程连接回池，并补充连接池环境变量模板。
- [x] BUG-060 `deploy.sh` / `.env.template`：部署脚本读取 `GUNICORN_WORKERS` 生成 systemd `gunicorn -w` 参数，支持按环境线性放大并发处理能力。
- [x] BUG-061 `udid_hybrid_system.py`：初始化阶段 `ALTER TABLE` 改为 PostgreSQL `ADD COLUMN IF NOT EXISTS` 分支，修复“捕获重复列异常后事务中断”导致的后续 DDL 失败。

### J. 后台页面信息架构重整（2026-03-02）
- [x] BUG-062 `admin.html`：将单页堆叠布局重构为“5 分区工作台（总览/同步与向量/AI 配置/用户与权限/审计与安全）”，新增导航切换与任务流引导，保留原有 DOM ID 与 API 交互逻辑，降低操作复杂度。

### K. 一键启动端口清理（2026-03-02）
- [x] BUG-063 `start.sh`：启动前自动清理主服务与同步服务占用端口（默认 `8080/8888`，支持 `.env` 或环境变量覆盖），先优雅终止后在必要时强制终止，确保端口释放后再启动。
- [x] BUG-064 `start.sh`：启动流程增加 `.env` 自动加载与 PostgreSQL 密码预校验，避免环境变量未导出导致 `POSTGRES_PASSWORD` 误判为空并触发启动失败堆栈。

### L. PostgreSQL 布尔类型兼容修复（2026-03-02）
- [x] BUG-065 `udid_server.py`：`users.is_active` 全链路写入统一为布尔值（默认管理员创建、用户创建、用户状态更新），并将认证表定义对齐为 `BOOLEAN DEFAULT TRUE`，修复 PostgreSQL 启动时报错“boolean vs integer”。

### M. PostgreSQL 连接池耗尽治理（2026-03-02）
- [x] BUG-066 `db_backend.py` / `.env.template`：连接池获取改为“池满短轮询等待 + 超时失败”（默认 10s，可配置 `POSTGRES_POOL_ACQUIRE_TIMEOUT`），修复并发请求尖峰下 `connection pool exhausted` 直接导致 500 的问题。

### N. PostgreSQL 全量数据回填闭环（2026-03-02）
- [x] BUG-067 `scripts/migrate_sqlite_to_postgres.py`：新增无 `pgloader` 依赖的 SQLite→PostgreSQL 迁移脚本（`products`、`embeddings` 元数据、`sync_log`），用于本地/线上统一回填路径并支持幂等重跑。
- [x] BUG-068 `scripts/setup_postgres.sql`：`products.cert_no` 从 `VARCHAR(200)` 调整为 `TEXT`；同步对在线 PostgreSQL 执行 `ALTER TABLE ... TYPE TEXT`，修复迁移中 `StringDataRightTruncation` 中断问题。
- [x] BUG-069 `scripts/migrate_sqlite_to_postgres.py`：迁移收尾自动刷新 `stats_cache(total_products/manufacturers_count)`，避免迁移后首次 `/api/stats` 慢查询造成页面长时间显示异常。

### O. PostgreSQL 向量链路一致性加固（2026-03-02）
- [x] BUG-070 `embedding_service.py`：增量构建逻辑新增 `e.embedding IS NULL` 兜底重建；`build_embeddings` 改为按失败数返回真实 `success/partial`，修复“全失败仍 success=true”。
- [x] BUG-071 `embedding_batch.py`：`import_results` 空文件/全失败改为 `success=false`；`check_and_import_completed_tasks` 仅在导入成功时标记 `imported`；`process_embedding_queue` 返回真实 `success/partial`，并要求 `text_hash` 相同且 `embedding` 非空才可跳过重算。
- [x] BUG-072 `auto_sync.py`：`run_vector_sync_batch` 增加原子认领 `pending->processing`、停止/超时回滚与失败回写、空文本显式收口、结果缺失记录标记失败，修复批处理链路“假成功”和状态悬挂风险。
- [x] BUG-073 `sync_server.py`：后台同步线程 `finally` 显式调用 `LocalDataLake.release_thread_connection()`，闭环 PostgreSQL 连接释放。
- [x] BUG-074 `udid_hybrid_system.py`：队列入队改为先删后插（每个 `di_code` 单行状态），并在初始化时按 `di_code` 去重，消除 `UNIQUE(di_code,status)` 导致的状态迁移冲突。
- [x] BUG-075 `embedding_faiss.py`：构建索引仅使用 `embedding IS NOT NULL` 记录并跳过空向量，避免 `np.frombuffer(None)` 异常。
- [x] BUG-076 `scripts/migrate_sqlite_to_postgres.py`：迁移脚本默认迁移 `embedding` BLOB，改为可选参数 `--skip-embedding-blob` 才跳过，避免默认回填后向量全空。

### P. PostgreSQL 向量回填阻塞（2026-03-02）
- [x] BUG-077 `scripts/migrate_sqlite_to_postgres.py`：`embeddings.embedding` 全量回填已完成，PostgreSQL `non_null=2,629,201 / 2,629,201`，此前 `DiskFull` 阻塞已解除。

### Q. PostgreSQL 监控口径与联动启动修复（2026-03-02）
- [x] BUG-078 `udid_server.py` / `auto_sync.py` / `sync_server.py`：向量覆盖率统一按 `embeddings.embedding IS NOT NULL` 统计，修复“有行无向量却显示 100% 覆盖”的口径失真。
- [x] BUG-079 `auto_sync.py`：`run_vector_sync_batch` 在“队列为空但存在向量缺口”时改为 Fail-Fast 返回错误，不再误报“没有待处理向量”。
- [x] BUG-080 `sync_server.py`：统一 JSON 序列化 `datetime/date -> ISO8601`，修复 PostgreSQL 下 `/api/status` 与 `/api/sync/history` 500。
- [x] BUG-081 `start.sh`：一键启动改为联动启动 `sync_server`（端口健康检查 + 退出时联动清理），并在启动前校验 `SYNC_API_KEY` 强度。
- [x] BUG-082 `sync_monitor.html`：状态接口新增响应契约校验，`success=false`/格式异常统一进入错误态兜底，避免页面抛异常后展示全 0 误导信息。
- [x] BUG-083 `embedding_batch.py` / `embedding_service.py`：增量批处理候选集改为包含 `embedding IS NULL` 的缺失向量记录，统计口径同步改为仅计 `embedding IS NOT NULL`，修复“缺失向量被视为已完成导致无法补齐”的问题。

### R. PostgreSQL 全量迁移补齐（2026-03-02）
- [x] BUG-084 `scripts/migrate_sqlite_to_postgres.py`：扩展为全量迁移模式，新增 `system_config/users/auth_audit/data_audit_log/embedding_update_queue/ingest_rejects/sync_history/sync_run` 的 UPSERT 迁移与序列对齐，补齐“仅迁主表未迁配置与审计表”的缺口。
- [x] BUG-085 数据执行：已完成 SQLite→PostgreSQL 全量数据补迁与对账（上述业务表计数 1:1），并完成 `embeddings.embedding` 全量回填；`system_config` 六项关键配置（含 `api_key/embedding_api_key`）已同步到 PostgreSQL。

### S. PostgreSQL 语义检索恢复（2026-03-02）
- [x] BUG-086 `embedding_faiss.py`：新增旧索引自动迁移路径（`id_map.pkl -> id_map.json + manifest.json`），并在加载前做 `index.ntotal == map_count` 一致性校验，修复遗留索引被拒载导致 FAISS 全量失效的问题。
- [x] BUG-087 `db_backend.py`：PostgreSQL SQL 翻译层新增 `%` 字面量转义（保留 `%s/%%/%(name)s`），修复含 `%` SQL 在 psycopg2 pyformat 下触发 `tuple index out of range` 的兼容性问题。

### T. AI 配置优先级与语义排序精度修复（2026-03-02）
- [x] BUG-088 `config_utils.py` / `ai_service.py` / `embedding_service.py` / `embedding_batch.py`：新增 `env_overrides_db` 合并策略并将 AI/Embedding 链路切换为“数据库优先、环境变量兜底”，修复 `.env` 覆盖数据库配置导致 `parse-bid` 走错 API Base/Model（进而 401）的配置漂移问题。
- [x] BUG-089 `embedding_service.py`：宽泛产品名场景（候选池 >500）下，向量召回改为仅使用 `requirement` 文本，不再强制拼接 `product_name`，修复“宽词污染向量查询导致结果偏离用途语义”的问题。
- [x] BUG-090 `ai_service.py`：AI 调用失败日志增加密钥脱敏（`sk-*`/Bearer token），避免错误响应日志泄露真实凭据片段。

### U. AI 失败诊断可观测性增强（2026-03-02）
- [x] BUG-091 `ai_service.py`：新增最近一次 AI 调用错误快照（错误类型/状态码/上游地址），并在异常路径区分 `ssl_error/timeout/auth_error/rate_limit/upstream_unavailable/response_parse_error`，避免“统一无响应”误导排障。
- [x] BUG-092 `udid_server.py`：`/api/parse-bid` 与 `/api/test-ai` 失败分支接入结构化错误映射，返回可定位错误文案（TLS 握手失败/鉴权失败/限流/超时），并透出非敏感诊断字段。

## 备注
- 本文件用于持续记录修复计划与执行状态，后续新增问题按 `BUG-093+` 继续追加并标记。
